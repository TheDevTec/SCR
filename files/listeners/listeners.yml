chat:
  enabled: true
  # LOWEST, LOW, NORMAL, HIGH, HIGHEST, MONITOR
  priority: LOWEST
  rules:
  - "antiAd-ip"

anvil:
  enabled: true
  # LOWEST, LOW, NORMAL, HIGH, HIGHEST, MONITOR
  priority: LOWEST
  rules:
  - "antiAd-ip"

sign:
  enabled: true
  # LOWEST, LOW, NORMAL, HIGH, HIGHEST, MONITOR
  priority: LOWEST
  rules:
  - "antiAd-ip"

book:
  enabled: true
  # LOWEST, LOW, NORMAL, HIGH, HIGHEST, MONITOR
  priority: LOWEST
  rules:
  - "antiAd-ip"

# If you don't need this, don't turn it on!
# It may affect performance
item-pickup:
  enabled: false
  # LOWEST, LOW, NORMAL, HIGH, HIGHEST, MONITOR
  priority: LOWEST
  rules:
  - "antiAd-ip"

# If you don't need this, don't turn it on!
# It may affect performance
item-drop:
  enabled: false
  # LOWEST, LOW, NORMAL, HIGH, HIGHEST, MONITOR
  priority: LOWEST
  rules:
  - "antiAd-ip"